/**
 * COMMENT MODEL
 *
 * Defines the data structure and validation rules for Comment entities.
 * Comments enable team collaboration by allowing users to discuss bugs,
 * provide clarifications, and share information during issue resolution.
 */
import { z } from "zod";
// Zod Schema for Comment validation and type safety
// Ensures comments contain meaningful content and proper references
export const commentSchema = z.object({
    // Primary key - auto-generated by database
    id: z.number().optional(),
    // Bug this comment belongs to - references Bugs table
    bugId: z.number(),
    // User who wrote the comment - references Users table
    userId: z.number(),
    // Comment content - must contain meaningful text
    content: z.string().min(2, "Comment cannot be empty"),
    // Timestamp - automatically set when comment is created
    createdAt: z.date().optional(),
    // Username - included in query results for display purposes
    // Not stored in database, populated by JOIN queries
    username: z.string().optional(),
});
//# sourceMappingURL=Comment.js.map